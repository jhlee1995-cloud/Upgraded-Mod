# Stage A (extraction) — minimal deps to avoid the dependency conflicts
# that plagued the old repo (robustbench/autoattack/numba-cuda). NOT needed.
torch>=2.0
torchvision>=0.15
numpy>=1.24
scikit-learn>=1.3
pillow>=9.0

# Stage B (frame/topology) + analysis
scipy>=1.10
matplotlib>=3.7
ripser
persim
